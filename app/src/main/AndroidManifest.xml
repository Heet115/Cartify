<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Cartify">
        
        <!-- Splash Activity as launcher -->
        <activity
            android:name=".activities.main.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.Cartify.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <!-- Login Activity -->
        <activity
            android:name=".activities.auth.LoginActivity"
            android:exported="false"
            android:theme="@style/Theme.Cartify.NoActionBar" />
        
        <!-- Register Activity -->
        <activity
            android:name=".activities.auth.RegisterActivity"
            android:exported="false"
            android:theme="@style/Theme.Cartify.NoActionBar" />
        
        <!-- Main Activity -->
        <activity
            android:name=".activities.main.MainActivity"
            android:exported="false"
            android:theme="@style/Theme.Cartify.NoActionBar" />
        
        <!-- Product Detail Activity -->
        <activity
            android:name=".activities.product.ProductDetailActivity"
            android:exported="false"
            android:theme="@style/Theme.Cartify.NoActionBar"
            android:parentActivityName=".activities.main.MainActivity" />
        
        <!-- Cart Activity -->
        <activity
            android:name=".activities.cart.CartActivity"
            android:exported="false"
            android:theme="@style/Theme.Cartify.NoActionBar"
            android:parentActivityName=".activities.main.MainActivity" />
        
        <!-- Order Confirmation Activity -->
        <activity
            android:name=".activities.order.OrderConfirmationActivity"
            android:exported="false"
            android:theme="@style/Theme.Cartify.NoActionBar"
            android:parentActivityName=".activities.cart.CartActivity" />
        
        <!-- Profile Activity -->
        <activity
            android:name=".activities.user.ProfileActivity"
            android:exported="false"
            android:theme="@style/Theme.Cartify.NoActionBar"
            android:parentActivityName=".activities.main.MainActivity" />
        
        <!-- Orders Activity -->
        <activity
            android:name=".activities.order.OrdersActivity"
            android:exported="false"
            android:theme="@style/Theme.Cartify.NoActionBar"
            android:parentActivityName=".activities.main.MainActivity" />
        
        <!-- Order Detail Activity -->
        <activity
            android:name=".activities.order.OrderDetailActivity"
            android:exported="false"
            android:theme="@style/Theme.Cartify.NoActionBar"
            android:parentActivityName=".activities.order.OrdersActivity" />
        
        <!-- Search Activity -->
        <activity
            android:name=".activities.product.SearchActivity"
            android:exported="false"
            android:theme="@style/Theme.Cartify.NoActionBar"
            android:parentActivityName=".activities.main.MainActivity" />
        

            
    </application>

</manifest>